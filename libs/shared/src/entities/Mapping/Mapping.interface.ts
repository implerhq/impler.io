export interface IMapping {
  name: string;
  key: string;
  columnHeading: string;
  isRequired: boolean;
}

export interface IMappingFinalize {
  name: string;
  key: string;
  columnHeading: string;
}
